name: Hello GitHub Actions

on: [push]

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print a message
        run: echo "Hello, GitHub Actions!"

jobs:
  say-hello-again:
    runs-on: ubuntu-latest
    steps:
      - name: Print a different message
        run: echo "Hello again, GitHub Actions!"
